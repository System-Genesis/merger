import { difference } from '../src/utils/difference';

describe('t', () => {
    it('should', () => {
        const a = difference({ a: 's' }, { b: 'd', a: 's' });
        console.log(a);
    });
});
